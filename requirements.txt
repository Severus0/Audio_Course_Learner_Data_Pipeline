faster-whisper>=1.0.0
llama-cpp-python>=0.2.75
fasttext>=0.9.2
tqdm>=4.66.0
# Critical: FastText is currently incompatible with NumPy 2.0+
numpy<2.0

# Linux/NVIDIA GPU dependencies (Required for auto-configuration)
nvidia-cublas-cu12; sys_platform == 'linux'
nvidia-cudnn-cu12; sys_platform == 'linux'
